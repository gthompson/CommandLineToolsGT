# This is a place to organize all useful command line tools, whether written in bash, Perl or Python.
# Only promote programs to the bin/ directory if they are useful. Other collections of candidate codes are in the collections/ directory.

